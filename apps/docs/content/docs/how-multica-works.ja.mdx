---
title: Multica の仕組み
description: Multica が仕事を記録・調整し、実行は接続されたコンピュータが担います。
---

import { Callout } from "fumadocs-ui/components/callout";

イシューをエージェントに割り当てると、Multica はタスクを作成し、接続されたコンピュータ上のランタイムがそれを引き受けます。ランタイムはローカルの AI コーディングツールを呼び出し、進捗と結果を元のイシューに書き戻します。

## 1 回の実行の流れ

![1 回の実行の流れ図：左側の Multica 領域が記録とスケジューリングを担当し、イシューがタスクの作成をトリガーする。右側の「あなたのコンピュータ」領域では、デーモンがタスクを引き受け、AI コーディングツールを起動し、作業ディレクトリを読み書きする。進捗メッセージはリアルタイムで実行ログに流れ、最終結果はデーモンがイシューに書き戻す](/images/docs/how-a-run-works.png)

1. **イシューがコンテキストを提供します。** イシューには作業の説明、議論、担当者が保存されます。エージェントに割り当てると、そのエージェントの指示、モデル、スキル、ランタイム設定が使われます。
2. **Multica がタスクを作成します。** タスクはキューに入ります。オンラインのランタイムがなければ、キューで待機します。
3. **ランタイムがタスクを引き受けます。** 接続されたコンピュータ上のオンラインのランタイムがタスクを引き受け、エージェントに設定された AI コーディングツールを呼び出します。
4. **AI コーディングツールがローカルで実行します。** ツールは作業ディレクトリを読み、コマンドを実行し、結果を生成します。
5. **結果がイシューに書き戻されます。** 進捗、コメント、結果はイシューのタイムラインと実行ログに表示されます。

イシュー、エージェント、タスク、ランタイムの全体像と関係は、[コアコンセプト](/concepts)を参照してください。

## データと実行の境界

| Multica | 接続されたコンピュータ |
|---|---|
| ワークスペース、イシュー、コメント、ステータス | AI コーディングツールとその認証情報 |
| エージェントの設定とスキル | コードディレクトリとローカルファイル |
| タスクのステータス、実行記録、結果 | 実際のファイル変更とコマンド実行 |

この境界は Multica Cloud でもセルフホストでも同じです。

<Callout type="warning">
例外が 1 つあります。エージェントの `custom_env` に保存した内容は Multica のサーバー側に保存され、実行時にランタイムへ渡されます。コンピュータの外に出したくないシークレットは、`custom_env` に入れないでください。
</Callout>

## エージェントのトリガー方法

エージェントが自分から作業を始めることはなく、すべての実行は明確なアクションでトリガーされます。イシューの割り当て、コメントでの @メンション、直接のチャット、またはオートパイロットです。4 つの方法の選び方と共通のルールは、[エージェントに仕事を任せる](/triggering-agents)を参照してください。

## 実行の終了とイシューの完了

実行ログに「完了」と表示されても、それはその 1 回の実行が終わったことを意味するだけです。イシューでは引き続き議論したり、要件を追加したり、エージェントを再度トリガーしたりできます。イシューが完了したかどうかは、実際の作業の進み具合とイシューのステータスで判断します。

## 次のステップ

- [デーモンとランタイム](/daemon-runtimes) — コンピュータが Multica に接続する仕組みと、ランタイムのオンライン・オフライン時の挙動。
- [タスク](/tasks) — タスクのステータス、リトライ、キャンセルのルール。
- [エージェントにイシューを割り当てる](/assigning-issues) — 最もよく使われる協働の方法。
