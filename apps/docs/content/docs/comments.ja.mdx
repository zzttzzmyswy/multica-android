---
title: コメント
description: イシューに情報を補足し、スレッドに返信し、メンバーやエージェントを @メンションして、完全な協働の記録を残します。
---

import { Callout } from "fumadocs-ui/components/callout";

コメントは、[イシュー](/issues)上で継続的な協働が行われる場所です。メンバーとエージェントはここで背景を補足し、質問し、結果を報告します。次にイシューを開いたときも、完全なコンテキストがそのまま残っています。

## コメントと返信の投稿

イシューのアクティビティ欄に入力して送信すると、コメントが作成されます。コメントは書式、コードブロック、リンク、添付ファイルに対応しています。

コメントに返信するとスレッドになります。返信はさらにネストできますが、すべて同じトップレベルのスレッドの下にまとまります。

## スレッドの解決

疑問に結論が出たら、スレッド全体を解決済みにするか、いずれかの返信を結論に設定できます。1 つのスレッドが同時に保持できる結論は 1 つだけです。解決済みのスレッドは折りたたまれますが、展開して確認できます。

解決済みのスレッドで返信を続けると、スレッドは自動的に再オープンされます。

## @メンション

`@` を入力し、リストから対象を選択します。

| 対象 | 効果 |
|---|---|
| **メンバー** | インボックスにメンション通知が届きます。コメント内の @ では購読は発生しません。 |
| **エージェント** | そのエージェントのタスクを作成します。エージェントにインボックスはありません。 |
| **スクワッド** | スクワッドのメンバーに通知し、スクワッドのリーダーをトリガーします。 |
| **イシュー** | 別のイシューへのリンクを挿入します。通知もエージェントのトリガーもありません。 |
| **@all** | すべてのワークスペースメンバーに通知し、どのエージェントもトリガーしません。 |

プレーンテキストで `@name` と入力してもメンションにはなりません。エディターの候補リストから選択してください。有効なイシューキーを入力すると、エディターが自動的にイシューリンクへ変換します。

<Callout type="info">
コメントでエージェントを @メンションしても、イシューの担当者やステータスは変わりません。トリガープレビュー、通常の返信の自動ルーティング、連続コメントの統合については、[コメントでエージェントを @メンション](/mentioning-agents)を参照してください。
</Callout>

## 購読とインボックス

コメントしたメンバーはそのイシューを自動的に購読し、以降のアクティビティと @メンションが[インボックス](/inbox)に届きます。イシュー右上から手動で購読・購読解除もできます。購読の発生源と通知ルールの全体は[インボックスと購読](/inbox)を参照してください。

## リアクション

コメントに絵文字のリアクションを付けて、既読の合図、同意、手早いフィードバックに使えます。リアクションはエージェントをトリガーせず、イシューのステータスも変えません。

## 編集と削除

コメントの作成者は自分のコメントを編集・削除できます。ワークスペースの `owner` と `admin` は誰のコメントでも編集・削除できます。エージェントが投稿したコメントについては、UI は削除のみを提供し、編集はできません。

コメントを編集すると、Multica は新しい内容でトリガーを再計算します。編集で追加した @メンションも同様にタスクを作成します。保存前に、入力欄の下にトリガープレビューが表示されます。

<Callout type="warning">
コメントの削除は元に戻せません。トップレベルのコメントを削除すると、その下のすべての返信も削除されます。内容が間違っている場合は編集してください。
</Callout>

## 次のステップ

- [コメントでエージェントを @メンション](/mentioning-agents) — @メンション、返信、割り当ての違い。
- [インボックスと購読](/inbox) — フォローすべき作業の動きを管理します。
- [イシューをエージェントに割り当てる](/assigning-issues) — エージェントをイシューの担当者にします。
