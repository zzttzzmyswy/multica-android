---
title: Conventions
description: Single source of truth for code naming, i18n translation glossary, and Chinese voice guide.
---

This page is the single source of truth for code naming, the i18n translation glossary, and the Chinese voice guide. Anything that used to live in `packages/views/locales/glossary.md` or in scattered comments now lives here.

If you write Multica code, change a translation, or write Chinese product copy, this is the page to reference.

---

## 1. Code naming

### Routes

Pre-workspace routes (the routes that exist before the user is in a workspace) MUST use either a single word or the `/{noun}/{verb}` pattern.

- ✅ `/login`, `/inbox`, `/workspaces/new`
- ❌ `/new-workspace`, `/create-team`, `/accept-invite`

Hyphenated word groups at the root collide with user-chosen workspace slugs and force endless reserved-slug audits. Reserving the noun (`workspaces`) automatically protects the entire `/workspaces/*` subtree.

### Workspace-scoped routes

Always live under `/{slug}/{section}` — `/{slug}/issues`, `/{slug}/agents`, `/{slug}/settings`. Never duplicate workspace routing logic; use `useNavigation().push()` from shared code, never framework-specific link APIs.

### Packages and modules

The monorepo enforces strict package boundaries:

| Package | May depend on | Must NOT depend on |
| --- | --- | --- |
| `packages/core` | nothing app-specific | `react-dom`, `localStorage`, `process.env`, `next/*`, UI libraries |
| `packages/ui` | nothing | `@multica/core`, business logic |
| `packages/views` | `core/`, `ui/` | `next/*`, `react-router-dom`, stores |
| `apps/web/platform/` | `next/*` | other apps |
| `apps/desktop/.../platform/` | `react-router-dom`, electron | other apps |
| `apps/mobile/` | types and pure functions from `@multica/core` | Web/Desktop React pages, stores, and platform implementations |

If logic appears in both apps, it MUST be extracted to a shared package. There are no exceptions for "small" duplication. Mobile owns a separate UI, data layer, and release process; it shares only types and pure functions.

### Files and components

- Files: `kebab-case.tsx` / `kebab-case.ts` (e.g. `agent-row-actions.tsx`)
- Components: `PascalCase` (e.g. `AgentRowActions`)
- Hooks: `useCamelCase` (e.g. `useWorkspaceId`)
- Tests: colocated as `<file>.test.ts(x)`
- Stores (Zustand): `<feature>-store.ts`, exported as `use<Feature>Store`

### Database (Go + sqlc)

- Tables: `snake_case` singular (`user`, `workspace`, `agent_runtime`)
- Columns: `snake_case` (`workspace_id`, `created_at`, `last_seen_at`)
- Foreign keys: `<table>_id`
- Booleans: `is_<state>` or `<state>_at` (timestamp form preferred for state changes)
- Migration files: `NNN_descriptive_name.up.sql` + `.down.sql` — always provide both directions
- Do not create database foreign keys or use cascade deletes or updates; enforce relationships and cleanup in the application layer.
- Every index must use `CREATE INDEX CONCURRENTLY` or `CREATE UNIQUE INDEX CONCURRENTLY`, with each concurrent index in its own single-statement migration file.

### Go

- Standard `gofmt` + `go vet`. No exceptions.
- Handler files mirror domain: `agent.go`, `auth.go`, `runtime.go`
- Tests: `<file>_test.go` colocated
- For UUID parsing in handlers, follow the rule in the root `CLAUDE.md` — `parseUUIDOrBadRequest` for boundary input, `parseUUID` (panicking) for trusted round-trips, never `util.ParseUUID` directly without checking the error.

### TypeScript

- API responses on the wire are `snake_case`; the api client converts to `camelCase` at the boundary. Inside TS code, **always camelCase**.
- Types: `PascalCase` (`Issue`, `AgentRuntime`); never `IPrefix`, never `_t` suffix.
- Enums: prefer string literal unions; reserve `enum` for runtime-iterable cases.
- TanStack Query keys: factory functions in `<feature>/queries.ts`, e.g. `issueKeys.detail(id)`.

### API boundaries

- Parse network responses with `parseWithFallback` and zod schemas from `packages/core/api/schema.ts`; do not cast them directly with `as T`.
- When adding or changing an endpoint, update its schema and cover missing or malformed fields in tests.
- Downstream UI must provide defaults for optional fields, and every `switch` over a server enum must include a `default` branch.
- An installed Desktop client may connect to a newer backend; never assume that frontend and backend versions always match.

### Runtime display names

`AgentRuntime.name` is the daemon's raw technical name (e.g. `Codex (host)`); the user's alias lives in `custom_name`. User-visible copy must never render `runtime.name` directly — use the shared helpers so aliases and provider stay consistent (MUL-5248, #5260):

- Standalone runtime label (lists, chips, confirm dialogs, document titles): `runtimeDisplayLabel(runtime)` → alias + provider, falling back to the daemon name.
- When a provider icon/text already sits beside it: `runtimeDisplayName(runtime)` → alias only, no duplicated provider.
- Inside a machine group: machine header uses `machine.title`, child rows use `runtimeRowLabel(runtime, machine.title)`.
- Runtime selectors group by machine via `buildRuntimeMachines`; do not build a flat list of raw names.

Raw `runtime.name` is allowed only for internal identity — hostname parsing, grouping, search haystacks, and protocol payloads — never for JSX text, i18n params, `<Select>` labels, or window titles.

### Issue keys

Every issue has a human-readable key like `MUL-123`: workspace `issue_prefix` (uppercase letters and digits, typically 3 chars, max 10) + sequence number. Workspace admins can change the prefix in Settings → General; changing it renumbers every existing issue, so external references that embed the old prefix (PR titles, branch names, links in docs and chat) stop resolving.

### Comments in code

English only. The repo enforces this for both Go and TypeScript. If you find a Chinese comment in code, it's a bug — replace it.

### Commit messages

Conventional format: `feat(scope)`, `fix(scope)`, `refactor(scope)`, `docs`, `test(scope)`, `chore(scope)`. Atomic commits grouped by intent.

---

## 2. i18n translation glossary

This is the **mandatory** glossary for every translation PR. It used to live at `packages/views/locales/glossary.md`; that file is now a stub pointing here.

### The core distinction: everyday noun vs Multica-specific term

Multica's product nouns split into two categories:

- **Everyday noun** — the word a user would say out loud for it. **Translate fully**, whether or not it is a database entity: `issue` → 任务, `workspace` → 工作区, `project` → 项目.
- **Multica-specific term** — a concept no local word carries (`skill`), or a schema-level identifier a user may have to type or match (`todo`, `in_progress`, `task_id`). Render as **lowercase English** so it reads like a type name.

The Chinese pages under `apps/docs/content/docs/*.zh.mdx` are the de facto voice standard for everything else on this page. They still carry the old English `issue` in prose and are being swept separately — follow the table below, not those pages, for this term.

### `issue` is the product's task — translate it

`issue` is the English product name for the thing a user files and an agent works on. In every other locale it is **the everyday word for "task"**:

| Entity | en | zh-Hans | ja | ko |
| --- | --- | --- | --- | --- |
| the filed unit of work (`issue`) | Issue | **任务** | **タスク** | **태스크** |
| one agent execution run (`task`) | Task | `task` | **作業** | **작업** |

Both nouns are user-visible and they are **not** the same thing — an issue can carry many runs. Never let one locale spell both with the same word, which is why the run keeps a visually distinct form in each locale.

What does not move:

- **API / DB fields** stay `issue` / `task` / `skill` everywhere: `issue_status`, `task_id`, `skill_uuid`.
- **Code references and literal commands** stay English: `multica issue ...`, the `/issue` Slack and Lark slash command.
- **`skill`** keeps lowercase English in Chinese text — a Multica-specific concept with no established Chinese term; titles may capitalize as `Skills`.
- **`issue` in the "problem" sense** (runtime health) is a plain noun, not the entity: `{{count}} issues` on a machine card is `{{count}} 个异常`, not `任务`.

**Why `issue` is translated while `skill` is not**: users file and read issues all day, and "issue" has no meaning in Chinese, Japanese, or Korean outside of dev jargon. The everyday task word is what people already say for this. `skill` is a Multica-specific concept where no local word carries the meaning.

Other product nouns follow the same "translate what has a settled local word" test:

- **`project` → "项目"**: settled mainstream Chinese word. Feishu / Tower / Teambition / PingCode / GitHub Projects — every Chinese product translates it. No product keeps `project` in Chinese context.
- **`autopilot` → "自动化"**: in Chinese, "autopilot" associates with Tesla's "自动驾驶" and doesn't match what the feature does (run tasks on a schedule). Notion and Feishu both use "自动化"; that's the industry consensus.

### Don't translate — brands and acronyms

| Category | Terms |
| --- | --- |
| Brands | **Multica**, GitHub, Slack, Google, Anthropic, OpenAI, Claude, Codex, Cursor, Linear, Jira |
| Acronyms | API, CLI, URL, SDK, OAuth, JWT, SSO, WebSocket, HTTP, JSON, YAML, SQL |

### Translate fully — concepts

| English | Chinese |
| --- | --- |
| Workspace | **工作区** |
| Agent | **智能体** |
| Project | **项目** |
| Autopilot | **自动化** |
| Daemon | **守护进程** |
| Runtime | **运行时** |
| Inbox | **收件箱** |
| Comment | **评论** |
| Reply | **回复** |
| Notifications | **通知** |
| Member | **成员** |
| Label | **标签** |
| Settings | **设置** |
| Onboarding | **上手引导** |

### Translate fully — generic UI words

| English | Chinese |
| --- | --- |
| Invite / Invitation | 邀请 |
| Search | 搜索 |
| Email | 邮箱 (label) / 邮件 (action) |
| Password | 密码 |
| Sign in / Log in | 登录 |
| Sign up | 注册 |
| Sign out / Log out | 退出登录 |
| Save / Cancel / Delete | 保存 / 取消 / 删除 |
| Confirm / Continue / Back | 确认 / 继续 / 返回 |
| Edit / New / Create / Add | 编辑 / 新建 / 创建 / 添加 |
| Remove / Send / Open / Close | 移除 / 发送 / 打开 / 关闭 |
| Preview / Download / Upload | 预览 / 下载 / 上传 |
| Done / Loading... | 完成 / 加载中... |
| Profile / Account / Appearance | 个人资料 / 账号 / 外观 |
| Theme / Language | 主题 / 语言 |
| Light / Dark / System | 浅色 / 深色 / 跟随系统 |
| Active / Archived | 活跃 (or 启用) / 已归档 |
| Status / Priority | 状态 / 优先级 |
| Assignee / Reporter | 负责人 / 报告人 |
| Description / Title | 描述 / 标题 |
| Date / Time | 日期 / 时间 |
| Today / Yesterday / Tomorrow | 今天 / 昨天 / 明天 |
| Empty / Failed / Success | 空 / 失败 / 成功 |
| Error / Warning | 错误 / 警告 |

### Roles and status enums (lowercase English, not translated)

These are schema-level identifiers; render as lowercase English even in Chinese context.

- Roles: `owner` / `admin` / `member`
- Issue status: `backlog` / `todo` / `in_progress` / `in_review` / `done` / `blocked` / `cancelled`

In UI, surface them in English (optionally `code-style` wrapped):

- "你需要 owner 权限"
- "已切换到 in_progress"

### Word combination rules

Always put **a single space** between an English word (entity / brand / acronym) and surrounding Chinese:

- "Create new issue" → "新建任务"（`任务` is Chinese, so no space)
- "Assign to agent" → "分配给智能体"
- "Configure runtime" → "配置运行时"
- "Stop daemon" → "停止守护进程"

### Plurals and counts

i18next uses `_one` / `_other`; Chinese has no grammatical number, only fill `_other`.

```json
// en/issues.json
{
  "issue_count_one": "{{count}} issue",
  "issue_count_other": "{{count}} issues"
}

// zh-Hans/issues.json
{
  "issue_count_other": "{{count}} 个任务"
}
```

Common count formats:

- `{{count}} issues` → `{{count}} 个任务`
- `{{count}} agents` → `{{count}} 个智能体`
- `{{count}} workspaces` → `{{count}} 个工作区`
- `{{count}} comments` → `{{count}} 条评论`
- `{{count}} members` → `{{count}} 位成员`
- `{{count}} skills` → `{{count}} 个 skill`

### Interpolation

Use `{{var}}`. Chinese translations may reorder for natural sentence flow.

```json
// en
{ "welcome_message": "Welcome back, {{name}}!" }

// zh-Hans
{ "welcome_message": "欢迎回来，{{name}}！" }
```

### Translation key naming

Three-level nesting: `feature.component.action`.

```json
{
  "feature_or_component": {
    "subcomponent_or_section": {
      "action_or_label": "..."
    }
  }
}
```

Examples:

- `issues.toolbar.batch_update_success`
- `issues.detail.comment_form.placeholder`
- `inbox.empty.title`
- `settings.preferences.language.title`

### Web-only / desktop-only copy

- Shared copy: top level of the namespace JSON
- Web-only: `web` section
- Desktop-only: `desktop` section

See `auth.json` for the canonical example (the `web` section contains `prefer_desktop` / `desktop_handoff.*`).

### ja / ko: derive the convention from the bundle, don't guess

The tables above are the **zh** glossary. ja and ko are not bound by them — they
render the same concepts with their own words (`Agent` is エージェント / 에이전트,
not 智能体), so a zh entry settles nothing for them.

For ja and ko the convention is **derived from the bundle**, and a term is only
settled when the bundle answers the question itself:

1. **A clear majority settles it.** If the bundle renders a term with one native
   word almost everywhere, the stragglers are debt — converge them and pin the
   majority in a guard. `instance` (10/11 ja, 8/11 ko → インスタンス / 인스턴스) and
   `provider` (20/23 ja, 19/23 ko → プロバイダー / 제공자) were settled this way.
2. **A clean partition by surface settles it.** ko renders a reply to an issue
   comment as 답글 and the agent's reply in chat as 답변, with zero crossover; that
   partition *is* the rule, so it is asserted rather than collapsed.
3. **No majority and no partition means undecided.** Report it and stop. Picking
   a winner there is a guess dressed up as a convention, and it is worse than
   leaving the split visible.

Terms outside these tables are held to the same rule, in
`packages/views/locales/ja-ko-unlisted-terms.test.ts`; the section-2 concepts are
in `ja-ko-concepts.test.ts`. Both carry the tally each derivation came from, so a
later reader can tell a deliberate reversal from a fresh outlier. The 148 round
added the unlisted-terms guard for `instance`, `desktop`, `provider`, `batch`,
`heartbeat`, `tier` and the `Pull request` casing. The 151 round re-ran that
guard's Latin-kept scan and found one of its two exclusions was a measurement
error: `Fleet` had been excluded because "ko also writes 플릿", but every hit is
`템플릿`, the word for *template*, which contains `플릿` as a substring. Both
bundles write the term in Latin zero-of-four times natively, so `Fleet` is a keep
and is now pinned. The `native` field is a RegExp rather than a string for the
same reason — the 150 round lost a measurement to a native pattern that contained
the Latin form it was meant to exclude, and this round lost one to a native form
that is a substring of an unrelated word. A substring check can express neither
distinction, and both mistakes returned a confident wrong answer rather than an
error.

Typographic conventions that are not terms live in
`packages/views/locales/ja-ko-typography.test.ts`. The 149 round measured the two
that 147 had recorded without measuring:

- **Parentheses.** 147 said ja uses full-width `（）` and ko half-width `()`.
  ko holds with zero exceptions (129 half-width pairs, 0 full-width). ja did not:
  66 full-width pairs against 19 half-width ones over 19 keys, with no partition
  behind them — the same key shape appeared both ways inside one namespace, and
  the content was native text 11 times and Latin 8 times, so neither content kind
  nor surface separated the forms. Those 19 were stragglers of a settled rule, so
  they were converged (85 full-width, 0 half-width) and both sides are pinned.
- **Figure and counter.** ko writes `45초`, `5분`, `30일`, `12건` and never
  `45 초` — 37 tight occurrences against 0 spaced on a literal figure, and 179
  tight against 0 spaced on a placeholder, so it is pinned on both halves. ja is
  *not* settled; see the table below.
- **The state form.** A value that is nothing but a state label is terminal in
  both locales: ja ends it in `〜済み` (51 keys, 0 in `〜された`) and ko in `〜됨`
  (62 keys, 0 in `〜된`). Neither state form is ever used as a passive predicate
  (`済み`+`可能性`, `済み`+`ます`, `됨`+`습니다`, `됨`+`하여`: 0 hits each). The 150
  round reached this from the ja suffix-composition scan; both claims hold at zero
  exceptions, so they are settled and pinned in
  `packages/views/locales/ja-ko-state-form.test.ts`. What is *not* claimed:
  everything inside a sentence does not take the other form. ja uses `〜済み` as a
  pre-nominal modifier (`削除済みエージェント`) and `〜された` as one
  (`保存されたビュー`), so the modifier position takes both and is not a partition.
  Only the terminal-label position is clean, and only that is asserted. Note the
  false straggler the guard also pins: `runtimes.machine.not_found_hint` writes
  `削除済み` where four siblings write `削除された`, but the English sources differ
  in construction (a participle in a list of states vs a passive clause) and the
  bundle honours the distinction. Do not converge it.
- **Notation inside a native word.** The 151 round asked the question underneath
  all of the above: once the bundle has chosen the native word, is it spelled the
  same way every time? A word can be settled and still written two ways, and a
  Latin-residue scan sees neither spelling. The axis is the long-vowel mark
  (`ブラウザ` / `ブラウザー`, `コンピュータ` / `コンピューター`) and the vowel itself
  (`デフォルト` / `ディフォルト`). Measured over the whole ja bundle, **each word is
  internally consistent except two**, so thirteen words are pinned at zero
  exceptions in `packages/views/locales/ja-ko-notation.test.ts`: サーバー (57
  occurrences), ユーザー (24), コンピュータ (33), ヘッダー (6), フッター (1),
  オーナー (15), マネージャー (1), エディタ (1), パラメータ (1), デフォルト (16),
  メンバー (95), ワークスペース (247), デスクトップ (19); ko adds 데스크톱 (19).
  The two exceptions were classified differently and both classifications are
  pinned:
  - **`フォルダ` — converged.** 8 `フォルダ` against 1 `フォルダー`, and the lone
    straggler sat on a *different surface* (`skills.detail.add_file.errors.is_directory`)
    from all seven majority keys (every one under `projects.resources.*`). No
    same-kind pair took both forms, so this is the first rule above — a clear
    majority with debt behind it — and the straggler was converged.
  - **`ブラウザ` — open, in the table below.** 11 against 2, and unlike `フォルダ`
    the minority is *inside the majority's own namespace*: `settings.shortcuts.reserved_error`
    writes `ブラウザ` while its two siblings
    `settings.shortcuts.actions.goBack.description` and `goForward.description`
    write `ブラウザー`. Same surface, same kind, two forms — so neither the
    majority rule nor a partition argument is available, and picking one would be
    a guess.

**Currently undecided — needs a locale owner's call, do not pick one:**

Each row is verified by `packages/views/locales/unsettled-ledger.test.ts`, which
records the competing forms, the same-kind keys that take different ones, the
**count caliber** each number is measured in, and who has to answer and at what
cost. It fails if a later round makes a split disappear without settling the term,
if a `why` stops stating its caliber, or if a row loses its `Ask` routing.

The 150 round added the `Ask` column because ten rounds of "still undecided" went
by with the table unable to say what anyone was being asked. It also re-measured
every number and found one wrong: the `Server` row's native count is 14, not 15 —
15 was `settings.mcp.*`'s number, borrowed into a sentence about a different
surface.

The 151 round turned every number in the table into a claim the guard re-measures.
The `collision` anchors catch "an anchor was rewritten" and the caliber prefix
catches "the sentence stopped saying what it counts"; neither catches "the family
grew a key" or "the number was measured over the wrong surface" — and both had
happened. `Server` repeated the 150 defect one entry over: "native in all 15 of the
20 keys that name a server" reads as 20 keys naming a server, where 20 is the size
of the `settings.mcp.*` surface and 15 is the number of keys in it that name one.
`Agent counter (ja)` was worse: "4 件 vs 2 個 ... ko is unanimous on 개 for all six"
came from a pattern that only saw the counter-after-placeholder position, which is
6 of the 18 ja keys that count an agent. The other 12 take 件 or 個 in the
placeholder-first position, or a third counter, `体`. Every entry now carries a
`facts` list — each number the `why` states, with the scope it was taken over — and
`unsettled-ledger.test.ts` re-derives all of them from the bundle. A round that
adds a key to any of these families now goes red instead of leaving a stale number
to be trusted.

| Term | Where | Why the bundle cannot settle it | Ask |
| --- | --- | --- | --- |
| `Server` | `agents.tab_body.mcp_config.*` | by key: 23 Latin vs 14 サーバー / 14 서버 inside that one surface (79 keys), alternating key by key, while `settings.mcp.*` names a server in 15 of its 20 keys and is native in all 15. ja and ko split identically, key for key | locale owner — word choice, 37 ja + 37 ko keys |
| `review` | ko | by key: 검토 9 vs 리뷰 8, and the status control (`issues.status.in_review`) disagrees with the prose that enumerates the same statuses | locale owner — word choice, 17 ko keys, visible in the status chip |
| `label` | ko | by key: 라벨 26 vs 레이블 25, no partition — the same modal takes both (`modals.create_issue.set_labels` is 레이블, its sibling toast is 라벨) | locale owner — word choice, 51 ko keys, no majority to lean on |
| `Roles in ja/ko prose` | ja / ko | by key: ja keeps lowercase Latin roles in permission prose (22 keys) but renders the role labels natively (130, overlapping in 5); ko splits inside one surface (`settings.mcp.admin_only_note` is Latin, `settings.workspace.manage_hint` is native) with Latin in 8 keys against 141 native. The section above does not say which applies | locale owner — rule scope, up to 152 ja + 141 ko keys |
| `Register (polite level)` | ko | by key: 856 values carry 습니다 and 398 an 해요체 ending (895 and 439 by occurrence), with no clean partition — `common.lark_bind.error_expired` and `common.slack_bind.error_expired` are the same string in two registers. See the note below | locale owner — whole-bundle register, 1254 ko keys |
| `Private` | ja | by key: 7 プライベート vs 9 非公開, split by neither surface nor label-vs-prose: `settings.plugins.private` and `settings.repositories.github_private` are both a bare `Private` label under settings and take different words | locale owner — word choice, 16 ja keys |
| `Figure and counter spacing` | ja | by occurrence: 42 spaced vs 14 tight on a literal figure, 155 vs 40 on a placeholder (both over every placeholder name), and the same English source renders both ways: `autopilots.relative_date.one_day_ago` is `1 日前` / `projects.relative_date.one_day_ago` is `1日前`, and `{{count}}mo ago` gives `{{count}} か月前` vs `{{count}}か月前` | typography owner — spacing, not a word choice, 211 occurrences |
| `Agent counter (ja)` | ja | by key: 9 件 vs 5 個 vs 4 体 over the 18 ja keys that count an agent, and the split is not source-side — the identical English source `{{count}} agents` takes 件 in two keys and 個 in two others, a 2:2 tie on one source — while ko is unanimous on 개 for all 18, so the split is ja-only | locale owner — counter choice, 18 ja keys |
| `Tool counter (ja)` | ja | by key: 1 件 vs 1 個 with no majority at all, the same English source (`{{count}} tools`) on both, ko unanimous on 개 — and the tight-spacing member is also a figure-spacing outlier above | locale owner — counter choice, 2 ja keys |
| `ブラウザ` | ja | by key: 11 ブラウザ vs 2 ブラウザー, and unlike the other katakana splits the 151 round measured, the minority sits inside the majority's own namespace — `settings.shortcuts.reserved_error` writes ブラウザ while its two siblings `settings.shortcuts.actions.goBack.description` and `goForward.description` write ブラウザー | locale owner — word notation, 13 ja keys |
| `Ellipsis` | both zh bundles | the §3 bullet contradicts itself — see the note there | locale owner — punctuation clause, 6 mobile + 52 views zh keys |

**Figure and counter spacing — the external-standard route is closed.** The 149
round flagged this as a candidate for settling from an external standard rather
than from the locale owner. Round 150 walked that route and it is empty. The two
sources that could plausibly speak to it were read:

1. 文化庁「新しい「公用文作成の要領」に向けて（報告）」(2021), Ⅰ-4「数字の使い方」 —
   the operative Japanese government writing standard. **The word 助数詞 does not
   appear anywhere in the report.** Its numeral section governs 算用数字 vs 漢数字,
   全角 vs 半角 (explicitly 「特に定めはない」 — no rule, only "be consistent"), the
   three-digit comma, 兆・億・万 in kanji, and counter *spelling* (「○か所」「○か月」
   in hiragana, explicitly rejecting 「３ヶ所」「７カ月」). It states no spacing rule.
   Its own model examples (「３か所」「７か月」) use a full-width digit, which is the
   report's own convention for single digits, so there is no Japanese/Latin
   boundary inside them and the question does not arise.
2. W3C JLReq (Requirements for Japanese Text Layout, the public form of the
   JIS X 4051 line of work). **助数詞 does not appear**; 算用数字 appears once, in
   the glossary. Its spacing rules — 二分アキ, 四分アキ, ベタ組 — are defined **only
   for punctuation classes** (始め括弧類, 終わり括弧類, 読点類, 句点類, 中点類).
   Nothing defines a figure-to-counter gap.

Both are **silent**, not in conflict. A standard that does not address the question
can neither confirm the 42-vs-14 majority nor override it, and citing it as if it
did would be inventing a rule and attributing it to the standard. The entry stays
open and stays routed to the typography owner. Do not re-walk this route without a
new source; the negative result is pinned in `unsettled-ledger.test.ts`.

What the round *did* settle from source 1 is adjacent: the counter spelling the
bundle already uses is the one that standard prescribes — `か月`, not `ヶ月`
(2 keys, 0 exceptions), so no change was needed there.

**ko register.** The 148 round measured this by sentence ending. The 149 round
re-measured it per key and got the same answer: 856 values carry 습니다 and 398 an
해요체 ending. It is not drift that a majority can fix: the split cuts through
every kind of key
(`error` / `required` leaves are 36 해요체 vs 34 습니다체, `common.*_bind.*` is 18 vs
5, and `settings.lark.install_error*` is the only family that is clean at 8 vs
0), and no namespace separates them (`common` is 78% 해요체, `chat` 8%). Two
readings are both defensible — 습니다체 as the house style with 139 stragglers, or
해요체 for friendly guidance and inline errors by design — and the bundle cannot
tell them apart. ja is *not* affected: it is 131 です・ます and 0 だ・である.

---

## 3. Chinese voice and style

### Punctuation

- Full-width punctuation in Chinese: `，。：；！？`
- Quotes: straight double quotes `"..."` to match the English source. Do not use `「」` or curly quotes.
- Brackets: full-width `（）`, in both bundles. **This rule is derived, not quoted** — the bullet above does not name brackets, and the first reading of the bundle was wrong. The tempting hypothesis is a partition by content kind (half-width wraps Latin, full-width wraps Chinese); it is false, because full-width brackets wrap a `{{binding}}` 25 times in views and 11 in mobile (`（{{count}}）`, `（Lark）`, `（Go + Postgres）`, `（xoxb-）`). What the bundle actually does is use full-width brackets everywhere — 74 pairs in views, 37 in mobile — against 4 and 1 half-width stragglers, two of which wrapped Chinese and three a placeholder or acronym. A clear majority with no partition behind the minority, so the stragglers were converged and the rule is pinned in `packages/views/locales/zh-typography.test.ts`.
- Ellipsis: three dots `...` not the single character `…`. Match the English source.
  - **This bullet contradicts itself, and the bundles have already drifted apart because of it.** The English source uses both (`…` 96 / `...` 16 in the mobile bundle, `…` 129 / `...` 106 in views), so "three dots" and "match the English source" cannot both hold. The two zh bundles have each picked a different reading: mobile zh follows the source (6 keys differ), while views zh follows the first clause (52 keys use `...` where EN uses `…`). No guard pins either, and no round has been authorised to choose. Decide this clause before translating against it.
- Mixed Chinese-English: a single space on each side of the English word (see Word combination rules).

The 151 round is the first to scan zh punctuation at all. Rounds 142–150 walked
ja and ko almost exclusively — the rules they settled (ja full-width parentheses,
ko half-width, ko figure-to-counter spacing) are ja/ko rules that say nothing
about zh — so the two bullets above had been in this contract with nothing
asserting them. The drift they forbid was 15 keys deep: 11 `「」` pairs, 3 curly
pairs, three half-width `,` and one half-width `;` in prose, and 5 half-width
bracket pairs. All were converged, and both the settled half and the derived
bracket rule are now pinned in `packages/views/locales/zh-typography.test.ts`. The
same round also found the first zh-side concept residue since round 144 — 18 keys
in views zh-Hans still rendering `Autopilot`, `Members` or the entity `issues` in
Latin — and added those three to the concept list in
`packages/views/locales/zh-glossary.test.ts`.

### Style principles

- **Concise and direct.** Avoid translation-ese: "对于 X 来说"、"作为 X"、"我们的"。
- **Error messages**: gentle but clear. "无法保存修改" beats "保存修改失败了！".
- **Buttons**: verb first, 2–4 characters. "取消"、"保存修改"、"立即同步".
- **Tooltips**: full short sentence. "复制链接到剪贴板".
- **Placeholders**: example-style. "输入任务标题...".

### Where to look when in doubt

When the glossary doesn't cover a term, look at:

1. `apps/docs/content/docs/*.zh.mdx` — the de facto Chinese voice standard, 20+ pages of consistent translation
2. `packages/views/locales/zh-Hans/auth.json` and `editor.json` — JSON structure + selector API patterns
3. `packages/views/auth/login-page.tsx` — component-level selector API call site
4. `packages/views/settings/components/preferences-tab.tsx` — language switcher reference

---

## Updating this page

If you change a rule here, also:

1. Apply it in the relevant locale JSONs / CLAUDE.md / docs page
2. Note the change in the PR description so reviewers know to look for downstream sweep

This page is the contract; nothing else overrides it.
